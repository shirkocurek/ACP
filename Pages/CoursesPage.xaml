<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="c971_mobile_application_development_using_c_sharp.Pages.CoursesPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:c971_mobile_application_development_using_c_sharp.Controls"
    Title="Courses">
  <Shell.TabBarIsVisible>false</Shell.TabBarIsVisible>

  <Grid RowDefinitions="*,Auto" Padding="{OnPlatform iOS='0,0,0,12', Android='0'}">

    <CollectionView Grid.Row="0" ItemsSource="{Binding Courses}" SelectionMode="None">
      <CollectionView.ItemTemplate>
        <DataTemplate>
          <Frame Margin="12,8" Padding="12">
            <Grid ColumnDefinitions="*,Auto">
              <VerticalStackLayout>
                <Label Text="{Binding Title}" FontAttributes="Bold" />
                <Label Text="{Binding StartDate, StringFormat='Start: {0:d}'}" />
                <Label Text="{Binding EndDate,   StringFormat='End:   {0:d}'}" />
                <Label Text="{Binding Status,     StringFormat='Status: {0}'}" />
              </VerticalStackLayout>
              <Button Grid.Column="1" Text="View"
                      Command="{Binding BindingContext.ViewCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}"
                      CommandParameter="{Binding .}" />
            </Grid>
          </Frame>
        </DataTemplate>
      </CollectionView.ItemTemplate>
    </CollectionView>

    <controls:BottomNavBar Grid.Row="1" />
  </Grid>
</ContentPage>

